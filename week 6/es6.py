#minMax

def minMAX(metric):

    val=int(input("valore: "))
    if val==-1:
        finito=True
        min=-1
        max=-1
    else:
        min=val
        max=val
        finito=False

    while not finito:
        if metric=="min":
            if val<min
                min=val
        if metric="MAX":
                    if val>MAX
                        MAX=VAL
if metric== "min":
    return res
def main():
    metric=input("min or MAX)")
    minMAX(metric)
    return
min()